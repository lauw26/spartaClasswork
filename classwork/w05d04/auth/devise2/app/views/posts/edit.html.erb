<h1>Editing Post</h1>

<% if current_user.id == @post.user_id %>
	<%= render 'form', post: @post %>
<% else %>
	<h3>User not valid to edit this post</h3>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>